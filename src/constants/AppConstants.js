'use strict';
import keyMirror from 'keymirror';

const c = keyMirror({
  UPDATE_USER_PROGRESS: null,
  RECEIVE_PRODUCTS: null,
  CREATE_CART_ITEMS: null,
  UPDATE_CART_ITEMS: null,
  DESTROY_CART_ITEMS: null,
  RECEIVE_CART_ITEMS: null
});

export default c;